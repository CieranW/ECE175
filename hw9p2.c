#include <stdio.h>
#include <string.h>
#include <stdbool.h>
#define MAXLINE 1000
bool lineSearch(char c[], char t[]);

int main(void)
{

    // your code here.
    return (0);
}

bool lineSearch(char c[], char t[])
{

    // function that returns true if t is found in c.
}
